from setuptools import setup, find_packages

setup(
    name="src",
    version="0.0.1",
    description="its a laptop price prediction package",
    author="sid",
    packages=find_packages(),
    license="MIT"
)
 